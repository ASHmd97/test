<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:background="@android:color/transparent"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">
    <LinearLayout
        android:gravity="center"
        android:layout_gravity="center"
        android:orientation="vertical"
        android:background="@drawable/passport_loading_dialog_bg"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:minWidth="118dp"
        android:paddingStart="18dp"
        android:paddingEnd="18dp">
        <ProgressBar
            android:id="@+id/passport_loading_icon"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_marginTop="24dp"
            android:indeterminate="true"
            android:indeterminateDrawable="@drawable/passport_loading_dialog_icon_bg"
            android:indeterminateDuration="3000"/>
        <TextView
            android:textColor="@android:color/white"
            android:ellipsize="end"
            android:gravity="bottom|center_horizontal"
            android:id="@+id/passport_loading_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:layout_marginBottom="24dp"
            android:maxWidth="200dp"
            android:maxLines="3"/>
    </LinearLayout>
</FrameLayout>
